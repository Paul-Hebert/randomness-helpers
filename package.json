{
  "name": "seeded-helpers",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "start": "cd docs && astro dev && cd ..",
    "build": "cd docs && astro build && cd .."
  },
  "dependencies": {
    "astro": "^2.0.14",
    "random-seed": "^0.3.0"
  },
  "devDependencies": {
    "jest": "^29.4.3"
  }
}

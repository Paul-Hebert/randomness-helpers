import { randomDegree } from "../../geometry/degree";
import { randomPercent } from "../../number/percent";

export const randomHue = randomDegree;
export const randomSaturation = randomPercent;
export const randomLightness = randomPercent;
export const randomAlpha = randomPercent;
